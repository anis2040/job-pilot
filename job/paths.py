from __future__ import annotations
from pathlib import Path

# Project root (the directory containing the `job/` package).
# Single source of truth for locating .env, skill dirs, and profiles.
BASE = Path(__file__).parent.parent
